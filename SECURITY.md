# Security Policy

## 支持范围

当前首版定位为本机 Observer 工具，默认只监听 `127.0.0.1`。没有身份认证的实例不应直接暴露到公网或不可信局域网。

## 报告问题

请通过仓库的私密安全报告功能提交漏洞，不要在公开 Issue 中披露可利用细节。报告中请包含受影响版本、复现步骤、潜在影响和建议缓解方式。

重点关注以下边界：

- 注册项目路径或 Artifact 路径越过项目根目录
- 符号链接导致读取未授权文件
- 构造超大 JSON、JSONL、Markdown 或 Git Diff 造成资源耗尽
- Git 参数注入或任意命令执行
- Observer 模式意外写入被监控项目
- 未认证服务被远程访问

服务会拒绝把 SQLite 数据库放进被监控项目，并以私有权限创建缓存目录与文件。任务、Context、Session、文档、活动和 Git 输出都有单项及项目级上限。默认 Host 防护只面向本机部署；如需团队访问，必须在可信反向代理后增加认证与访问控制。
